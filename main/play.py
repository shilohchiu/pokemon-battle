if __name__ == "__main__":
    # say hello and display a list of possible pokemon to choose from
    # allow the player to pick 3 different pokemon
    # autogenerate 3 different pokemon from the cpu
    # allow the computer to pick the computer's team (random generated)
    
    #start battle
    # maybe build something that figures out the smartest attack to use/
    # implements some strategy by the cpu (maybe switch out for more prefereable 
    # pokemon if possible or find the highest expected payoff and pick that specific pokemon)
    # (best-opponent.find(your_pokemon), scans in list of constants and looks for that opponent's weakness)